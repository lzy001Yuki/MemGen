"""
tracking
========

Second benchmark family for this repo.

Goal: tasks that require *tracking / ordering* across multiple pick-and-place
subgoals in a single long-horizon episode (e.g., put multiple objects into a
fixture in order, then retrieve them in order).
"""

